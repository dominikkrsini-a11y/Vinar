{
  "name": "Vinar",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "npm ci\nnpm ci --prefix server",
  "terminals": [
    {
      "name": "assistant-proxy",
      "command": "./scripts/cloud-agent-assistant.sh",
      "description": "Vinar Express assistant proxy on port 3001. Uses Cloud Agent secrets when present; otherwise throwaway local credentials so GET /healthz works."
    }
  ],
  "ports": [
    {
      "name": "assistant-proxy",
      "port": 3001
    }
  ]
}
